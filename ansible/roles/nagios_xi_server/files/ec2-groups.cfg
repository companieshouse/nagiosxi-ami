define hostgroup{
        hostgroup_name          EWF-Servers
        alias                   EWF Servers
}

define hostgroup{
        hostgroup_name          XML-Servers
        alias                   XML Servers
}

define hostgroup{
        hostgroup_name          Email-Servers
        alias                   Email Servers
}

define hostgroup{
        hostgroup_name          Backend-Servers
        alias                   Backend Servers
}

define hostgroup{
        hostgroup_name          Oracle-Servers
        alias                   Oracle Servers
}

define hostgroup{
        hostgroup_name          Staffware-Servers
        alias                   Staffware Servers
}
